# Services (Listings) Feature

This folder will contain all logic related to creating, viewing, and managing service listings by sellers.

- UI Components (Listing card, creation form, detail view)
- Server Actions (createListing, updateListing)
- Data fetching functions (getListingById, getAllListings)